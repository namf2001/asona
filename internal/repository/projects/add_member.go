package projects

import (
	"context"

	"asona/internal/model"
)

// AddMember adds a existing user as a member of a project.
func (i impl) AddMember(ctx context.Context, member model.ProjectMember) error {
	return nil
}
